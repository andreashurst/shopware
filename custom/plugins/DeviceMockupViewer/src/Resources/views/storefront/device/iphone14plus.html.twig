{% extends "@DeviceMockupViewer/storefront/layout/base.html.twig" %}

{% block title %}iPhone 14 Plus Mockup{% endblock %}

{% block stylesheets %}
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    }

    .device-container {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        width: 100%;
    }

    .iphone-14-plus {
        width: 430px;
        height: 930px;
        background: linear-gradient(145deg, #ffe066, #ffcc02);
        border-radius: 52px;
        padding: 10px;
        position: relative;
        box-shadow: 
            0 0 0 3px #ffcc02,
            0 25px 50px rgba(0, 0, 0, 0.5),
            inset 0 2px 4px rgba(255, 255, 255, 0.3);
    }

    .iphone-14-plus::before {
        content: '';
        position: absolute;
        top: 70px;
        left: -3px;
        width: 6px;
        height: 70px;
        background: #ffcc02;
        border-radius: 3px 0 0 3px;
    }

    .iphone-14-plus::after {
        content: '';
        position: absolute;
        top: 160px;
        left: -3px;
        width: 6px;
        height: 90px;
        background: #ffcc02;
        border-radius: 3px 0 0 3px;
    }

    .power-button {
        position: absolute;
        top: 200px;
        right: -3px;
        width: 6px;
        height: 80px;
        background: #ffcc02;
        border-radius: 0 3px 3px 0;
    }

    .screen {
        width: 100%;
        height: 100%;
        background: #000;
        border-radius: 42px;
        overflow: hidden;
        position: relative;
    }

    .notch {
        position: absolute;
        top: 12px;
        left: 50%;
        transform: translateX(-50%);
        width: 140px;
        height: 30px;
        background: #000;
        border-radius: 0 0 18px 18px;
        z-index: 100;
    }

    .screen iframe {
        width: 100%;
        height: 100%;
        border: none;
        border-radius: 42px;
    }

    @media (max-width: 480px) {
        .iphone-14-plus {
            transform: scale(0.6);
        }
    }

    @media (max-width: 360px) {
        .iphone-14-plus {
            transform: scale(0.5);
        }
    }
</style>
{% endblock %}

{% block content %}
<div class="device-container">
    <div class="iphone-14-plus">
        <div class="power-button"></div>
        <div class="screen">
            <div class="notch"></div>
            <iframe src="{{ cleanUrl }}" title="iPhone 14 Plus Mockup"></iframe>
        </div>
    </div>
</div>
{% endblock %}